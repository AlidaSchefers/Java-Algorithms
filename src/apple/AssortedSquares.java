package apple;

import java.util.ArrayList;

public class AssortedSquares(int[] arr) {
    int[] arrEx = new int[]{-7, -3, -1, 0, 2, 5, 8, 15};
    //set i to first element's index
    int i = 0;
    //set j to the last element's index
    int j = 0;
    //initalize empty array newArr
    while(i != j){
        if(Math.pow(arr[i], 2) > Math.pow(arr[j], 2)){
            //add Math.pow(arr[i], 2) to the front of newArr
            //i++
        }else{
            //add Math.pow(arr[j], 2) to the front of newArr
            //j++
        }
    }
    //add Math.pow(arr[i], 2) to the front of newArr
    //return newArr





    //for each element in the array
        //square the element

}
